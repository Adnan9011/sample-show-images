<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.kiliaro">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:name="com.example.kiliaro.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:screenOrientation="portrait"
        android:theme="@style/Theme.Kiliaro">
        <activity
            android:name="com.example.kiliaro.presenter.splash.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.example.kiliaro.presenter.main.MainActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.Kiliaro"
            />
        <activity
            android:name="com.example.kiliaro.presenter.detailmedia.DetailMediaActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.Kiliaro"
            />
    </application>

</manifest>